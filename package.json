{
  "name": "blog",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "go run ./cmd/blog",
    "dev": "GIN_MODE=debug go run ./cmd/blog",
	"build": "npm --prefix admin ci && npm --prefix admin run build && node -e \"require('fs').mkdirSync('bin',{recursive:true})\" && go build -o bin/blog ./cmd/blog",
    "test": "go test ./..."
  }
}
